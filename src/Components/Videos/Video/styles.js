import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    box: {
        margin: '10px auto',
    },
    title: {
        cursor: 'pointer',
        color: '#000000',
    },
    thumbnail: {
        marginTop: '6px',
        cursor: 'pointer'
    }
});
