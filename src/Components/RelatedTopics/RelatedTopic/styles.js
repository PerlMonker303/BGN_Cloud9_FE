import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    topic: {
        padding: "8px 12px",
        backgroundColor: '#F0F8FF',
        color: 'black',
        cursor: 'pointer',
        margin: 'auto 6px',
        borderRadius: '8px'
    }
});
