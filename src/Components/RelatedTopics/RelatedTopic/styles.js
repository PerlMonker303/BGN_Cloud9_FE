import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    topic: {
        padding: "8px 12px",
        backgroundColor: 'gray',
        color: 'white',
        cursor: 'pointer',
        margin: 'auto 6px'
    }
});
