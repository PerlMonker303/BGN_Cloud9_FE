import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  paper: {
    margin: "auto",
    padding: "12px",
    marginBottom: "10px",
    border: "1px solid #3F83F4",
    borderRadius: "10px !important",
  },
});
